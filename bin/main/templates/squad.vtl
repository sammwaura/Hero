
<h1>$squad.getName()</h1>

<ul>
  #foreach($hero in $squad.getHero())
      <p>$hero.getName()</p>
      <p>$hero.getAge()</p>
      <p>$hero.getSuperPower()</p>
      <p>$hero.getWeakness()</p>
  #end
</ul>
<div class="well">
    <a href="/squads/$squad.getId()/hero/new">Add a new Hero</a>

    <a href="/squads">All Squads</a>
</div>